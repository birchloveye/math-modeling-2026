# 数学建模竞赛论文与科研制图规范

本规范用于 P9 写作、P10 审计和 P11 终审。参考优秀竞赛论文的表达方式，但禁止照搬文字、模型、数据或结论。

## 一、内容质量门槛

1. **先回答题目，再展示算法。** 每个小问必须形成“题意目标—模型定义—参数或约束—验证—结论—局限”的闭环。
2. **不得用流程正确代替模型正确。** Freeze、Claim 与哈希只能证明可追溯，不能证明统计假设合理。
3. **关键定义必须接受反例检查。** 首次达标、失效、异常标签等事件定义，应检查回落、重复测量、观察时点偏差、删失和标签代理误差。
4. **新增变量必须证明增量价值。** 问题要求考虑更多因素时，至少报告相对基线模型的折外指标差异及其重采样区间；区间跨零时不得宣称稳定改进。
5. **决策建议必须传播不确定性。** 分组切点、检测时点、阈值和风险权重应报告 Bootstrap、置信下界或敏感性结果；不可行必须明确标记，不得降低标准换取答案。
6. **类别不平衡任务不得只报准确率。** 至少报告 PR-AUC、灵敏度、特异度、PPV/精确率及校准误差；阈值只能在训练内层选择。
7. **冻结前执行“替代定义挑战”。** 若另一种合理的问题定义会实质改变结论，必须形成候选路线并进入人工 Gate，而不是直接冻结最先实现的路线。

## 二、论文版式

- A4；页边距不小于 2.5 cm；第一页为题目、摘要和关键词；不设目录；正文控制在竞赛上限内。
- 正文宋体小四（12 pt）；一级标题黑体小三附近，二级标题黑体四号附近；正文行距约 1.5 倍。
- 公式居中并连续编号，变量首次出现即解释；禁止把未解析的 LaTeX 命令直接输出到 PDF。
- 表格采用三线表，不使用大面积彩色底纹；小数位按指标精度统一；单位写入表头。
- 图题置于图下，表题置于表上；正文必须解释每幅图“看到了什么、为何支持结论”。
- 页码置于页脚中央；保持匿名；参考文献按正文首次引用顺序排列并提供可核验出版信息。
- 附录列出复现命令、环境、随机种子、文件清单与完整核心源码；正文与附录页数分开核对。

## 三、Python 科研制图

### 字号和清晰度

- 正文为 12 pt 时，图内正文使用 10.5 pt，刻度 9.5 pt，图例 9 pt；避免自动缩图后不可读。
- 位图最终输出至少 600 dpi；优先保留 PDF/SVG 矢量版本。坐标轴宽约 0.9 pt，主曲线约 1.2–1.5 pt，二者视觉重量接近。
- 坐标轴格式为“变量名(单位)”，例如“种植面积(亩)”；无单位时不加括号。

### 配色

- 多序列依次使用 `#1f77b4`（深蓝）、`#ff7f0e`（橙）、`#2ca02c`（绿）、`#d62728`（暗红）、`#9467bd`（紫）。避免亮粉、荧光绿和亮黄。
- 两组柱状对比使用深蓝与橙；类别超过颜色承载能力时配合点形、线型或纹理，不只依赖颜色。
- 有正负或中心零点的热力图使用 `coolwarm`；非负连续量使用 `viridis`；禁止使用 `jet`。
- 散点类别优先深蓝、橙、绿，并用点形或大小提供冗余编码。非重点点降低饱和度与透明度。

### 多子图规则

- 两图或四图共享坐标语义时，仅第一列保留 Y 轴标签，仅最下行保留 X 轴标签。
- 子图编号统一使用 `(a)`、`(b)`；图例不遮挡数据，网格线浅于数据线。
- 同一指标跨子图比较时统一坐标范围；颜色在全文保持固定语义，并检查灰度打印可辨性。

## 四、P10/P11 逐页检查

逐页渲染最终 PDF，检查：匿名信息、页边距、字体、公式、图表清晰度、交叉引用、孤行、裁切、重叠、空白页和附录完整性。审稿报告还必须分别列出“内容充分性”和“视觉质量”，不得以排版美观掩盖模型证据不足。
