# Agent 运行契约

本仓库采用人在回路工作流。执行任何阶段前，必须先阅读 `WORKFLOW.md`、`SKILL_ROUTING.md` 和当前阶段对应的 Skill。只加载路由表中当前阶段必需或满足条件的 Skill。

## 不可绕过的控制规则

- 到达任何人工 Gate 时必须停止。不得把沉默或上一阶段完成视为批准。
- P1 只生成 `workspace/problem/problem_contract.json`，随后停止。P3 只生成差异明显的模型卡，随后停止。仅允许一至两条由人工选定的路线继续。
- `workspace/experiments/FROZEN_RESULTS.json` 是最终数值结果的唯一来源。任何已接受的输入、模型、实验结果、表格或图形发生变化，都会使原 Freeze 失效，必须重新验证并冻结。
- 论文中的每项重要结论必须进入 Claim Ledger。`draft` 和 `rejected` 状态不得作为事实写入论文；`supported` 只能表述为暂定结论；只有 `verified` 可以支撑最终结论。
- 禁止编造指标、表格、图形、引用、实验或证据。缺少必要 artifact 或溯源信息时，必须按失败处理。
- 验证必须感知题型。只加载适用检查，不得强制要求任意数量的模型、图片、算法或消融实验。
- 参数扫描、数据摘要、哈希和一致性检查优先交给确定性的 Python 程序。Codex 默认读取紧凑的机器可读摘要，而非完整大型数据集。
- 保留复现实验所需的随机种子、配置、运行元数据和日志。
- 外部辅助 Skill 不得改变人工 Gate、标准实验目录、Claim Ledger 或 Freeze 的约束；发生冲突时以本文件为准。

## 推理预算

| 预算 | 用途 |
|---|---|
| LOW | 机械检查、文件操作、Schema 与格式校验 |
| MEDIUM | 数据摘要和结果解释、论文结构设计 |
| HIGH | 题意分析、候选模型设计、数学推导、关键异常解释、最终审稿 |
| ZERO-AI | 可由人工快速完成的路线淘汰和最终决策 |

不要把推理 Token 消耗在确定性计算上。先生成实验计划再批量运行；先生成大文件摘要再进行解释。

## 工作规则

- JSON 统一使用 UTF-8，并尽可能保持稳定排序。
- Schema 错误必须显式报告，禁止静默修复或强制转换。
- 明确记录假设与不确定性。
- 不得覆盖被冻结的证据。需要修改时创建新实验，或显式宣布旧 Freeze 失效后重新生成。
- P12 始终由人工提交；Codex 可以审计，但不得代替人工提交。

## 分支与改进留痕

- 每次正式赛题协作必须从已批准基线创建独立 `experiment/<problem-id>` 或 `benchmark/<problem-id>` 分支，题目 artifact、实验代码与阶段记录只提交到该分支。
- 工作中发现仓库自身不足时，不得把修复与赛题实验混成一个不可辨识提交。先从当前基线创建 `feature/<improvement>` 分支，记录问题、最小修复和验证证据，再合并或择取到赛题分支。
- 每项仓库改进至少包含一个聚焦提交；能够自动验证时必须附测试。提交信息应区分 workflow 改进与 problem-specific 工作。
- 未经人工批准，不得把赛题分支或功能分支合并到 `develop` 或 `main`。禁止强制推送覆盖已有历史。

