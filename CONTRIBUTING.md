# 参与贡献

使用 Python 3.11 或更高版本。命令行工具应尽量减少依赖、保持确定性、完整支持 UTF-8，并在出错时明确失败。提交变更前运行：

```bash
python -m unittest discover -s tests -v
```

修改 Schema 时，必须同步更新对应 Skill 与模板。修改 Freeze artifact 或验证逻辑时，需要提供可复现示例，以及覆盖接受与拒绝路径的测试。不得加入“必须生成固定数量图片、模型或实验”等代理目标。

从 `develop` 创建聚焦的临时分支。提交应保持范围清晰，不要纳入生成数据，并记录面向用户的行为变化。未经明确设计讨论，任何贡献都不得削弱人工 Gate、证据溯源或失败关闭检查。

